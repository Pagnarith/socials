{
  "name": "socials-telegram-bot",
  "version": "1.0.0",
  "description": "Telegram bot for automated messaging and subscriber engagement",
  "main": "src/bot.js",
  "type": "module",
  "scripts": {
    "start": "node src/bot.js",
    "dev": "node --watch src/bot.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:cron-auth": "vitest run tests/cron-auth.test.js"
  },
  "dependencies": {
    "dotenv": "^16.4.0",
    "googleapis": "^118.0.0",
    "node-cron": "^3.0.3",
    "telegraf": "^4.16.0"
  },
  "devDependencies": {
    "vitest": "^4.1.1"
  }
}
