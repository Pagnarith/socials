# Promo Studio — copy to .env
PORT=8787
# Shared secret for agent + you (required in production)
PROMO_STUDIO_API_KEY=change-me-to-a-long-random-string
# Optional absolute ffmpeg path (default: ffmpeg on PATH)
# FFMPEG_PATH=/opt/homebrew/bin/ffmpeg
